---
import { listItems } from "./list-items"
---

<nav class="flex">
	<div class="logo"></div>
	<ul class="flex justify-between gap-2">
		{listItems.map((item) => 
		<li>
			<a href={item.name}>
				{item.name}
			</a>
		</li>
		)}
	</ul>
</nav>
